<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>NBA Big Board Creator</title>
    <link rel="stylesheet" href="css/normalize.css">
    <link rel="stylesheet" href="css/home.css">
</head>
<body>
    <header>
        <h1>NBA Big Board Creator</h1>

        <% if (user) { %>
            <form method="post" action="/auth/logout" id="logout">
                <input type="submit" value="logout">
            </form>
            <%} else { %>
                <a href="/auth/login">Login</a>
            <% } %>
    </header>
    <div class="container">
        <section class="list-types">
            <section class="ranked">
                <% if (user) { %>
                    <a href="/ranked/<%= user.userName%>">View your ranked board</a>
                <% } else { %>
                    <a href="/ranked">Create Ranked List</a>
                <% } %>
                
                <p>Create a big board</p>
            </section>
            <section class="tiered">
                <a href="/tierList">Create Tiered List</a>
                <p>Create a tiered list with this years prospects</p>
            </section>
            <section class="mock">
                <a href="/mock">Create a mock draft</a>
                <p>Create a mock draft based off of the NBA lottery odds</p>
            </section>
        </section>
    </div>
</body>
</html>